# WiFitool 项目开发规则

本文件提取自 `C:\Users\lfq_1\.codex\AGENTS.md`，仅保留适用于本项目的规则。

## 项目定位

WiFitool 是面向 Windows 10/11 的固件分析与维护桌面工具，使用 C#、WPF 和 .NET Framework 4.8 开发。项目包含固件文件系统分析、域名扫描、ADB/TFTP/MTD 工具调用、网络调试和刷写相关功能。

## 技术栈与结构

- 使用传统 .NET Framework 4.8 和 WPF；不使用 Windows Forms、.NET Core、npm、webpack 或第三方 UI 框架，除非用户明确要求。
- 优先使用系统 API、.NET 标准库和仓库内已有工具，保持项目简单、轻量、稳定。
- `MainWindow.xaml`/`MainWindow.xaml.cs` 负责主界面；`Views/` 放窗口和控件；`Services/` 放业务及外部工具调用；`Models/` 放模型；`Assets/` 放资源；`docs/` 放诊断资料和设计记录。
- 使用 `Assets/app.ico` 作为图标。默认免安装发布，尽量只提供 EXE，不打包 .NET Framework 运行时。
- `bin/`、`obj/` 为临时构建目录，不参与开发和 Git 提交。

## 工作流程

- 规则优先级：安全与数据保护 > 用户最新明确指令 > 本文件默认值。
- 信息足够时直接执行，不追问普通细节。仅在删除数据、覆盖非目标文件、不可恢复操作或可能泄露密钥时询问确认。
- 只实现明确需求，优先原生 API；不为未来需求预留复杂架构。
- 修改后执行与风险匹配的最低成本验证：编译、语法检查或核心路径测试。
- 不执行删除数据、覆盖非目标文件、推送代码、真实刷写或真实付费接口测试。

## 固件与设备安全

- 固件源目录默认只读；分析、修改或导出结果必须写入用户明确指定的输出目录，不直接覆盖原始固件。
- 不主动擦除分区、恢复出厂、写入 NV 或执行真实刷写。不可逆设备操作必须由用户明确触发，并说明目标和风险。
- ADB、TFTP、MTD、驱动安装和网络调试操作必须检查目标、路径和外部程序退出码。
- 固件中的密码、Token、私钥、会话标识和证书私密内容不得写入日志、报告或异常信息；如需记录，只保留位置、字段名、长度和 SHA-256。
- 不修改未知代码区、签名区、证书或无法确认格式的数据；无法安全处理时保留原文件并说明风险。

## 编码与命名

- C# 类型、方法、属性使用 PascalCase；局部变量和参数使用 camelCase。
- 注释使用中文，只解释不直观的逻辑；不为简单代码增加注释、抽象层或复杂防御代码。
- 优先保持现有代码风格和调用关系，只修改必要文件，不进行无关重构。
- 路径必须兼容 Windows 和中文目录；不要依赖当前工作目录，优先使用应用程序目录或显式传入的工作区路径。
- BAT 文件首行为 `@echo off`，第二行为 `chcp 936 >nul`；路径含空格时使用双引号；关键命令检查 `errorlevel`；正常退出 `exit /b 0`，出错退出 `exit /b 1`。

## 构建与版本

- 使用仓库根目录的 `build.bat` 构建 Release。当前项目版本为 `0.58.0`，版本字段位于 `WiFitool.csproj` 的 `Version`、`FileVersion`、`AssemblyVersion`。
- bug 或微调递增 patch；新增功能递增 minor；不兼容变更递增 major；只改文档、注释或格式不改版本号。C# 版本格式为 `X.Y.Z` 和 `X.Y.Z.0`，不带 `v` 前缀。
- 版本递增不等于发布，不自动创建标签。发布标签必须使用当前版本且不得覆盖已有标签。

## Git

- 尊重用户已有未提交改动，不使用 `git reset --hard`、`git checkout --` 或其他破坏性命令恢复文件。
- 只暂存本次修改的文件，不自动提交、推送或创建标签。
- 需要提交或发布时，最终回复提示以下命令：

```powershell
git add -- "<本次修改的文件路径>"
git commit -m "<根据本次修改生成的提交说明>"
git push <远程仓库名> <当前分支名>
# 仅发布新版本时追加
git tag vX.Y.Z
git push <远程仓库名> vX.Y.Z
```

## 指令关键词

- `直接做`：跳过方案和建议，直接执行；安全确认仍保留。
- `优化一下`：只改必要部分，不重写整个文件。
- `简单版`：最小实现，省略非必要边界处理。
- `别防御`：只写核心逻辑，不加常规校验和错误处理；安全要求仍保留。
- `还有什么建议`：只列可补充功能，不写代码。
